#include<bits/stdc++.h>
using namespace std;

int main(){

    string s1 = "abc";
    string s2 = "xyz";
    cout<<s1.compare(s2);

    // if both the strings are equal the it will print 0
    // if s1 > s2 == +ve
    // if s1 < s2 == -ve
    // here we compare the precedance of the characters
    
    return 0;
}